name: CI/CD Pipeline Innovatech

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar el código del repositorio
        uses: actions/checkout@v3

      - name: Mensaje de prueba
        run: echo "¡El pipeline de GitHub Actions se ha activado correctamente!"
